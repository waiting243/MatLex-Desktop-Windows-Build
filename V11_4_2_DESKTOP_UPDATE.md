# MatLex Desktop v11.4.2

本次只针对桌面适配继续修正。

1. 面试问答和概念复习右侧学习概览改为受视口约束的自适应面板，标题、统计和底部操作始终可见，分类进度在面板内部滚动。
2. 不再只展示前6个分类，所有分类都能在右侧面板中查看。
3. 在980到1179像素宽的窄桌面窗口中自动改为上下布局，禁止右侧面板被窗口裁切。
4. 专业英语右侧设置区增加视口约束和内部滚动，窗口缩小时也不会显示不全。
5. 桌面新增的学习概览、窗口控件、返回上级等组件统一使用全局 MatLex Liquid Glass 参数，不再单独使用高不透明度白色玻璃。
